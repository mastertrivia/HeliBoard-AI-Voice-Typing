# Pin the Robolectric SDK for local runs on JDK 17.
# Robolectric 4.16 defaults to the highest supported SDK (36), which requires
# Java 21; this machine only has JDK 17. SDK 34 is fully compatible with JDK 17
# and behaves identically for the JSON-parsing / suggestion tests here.
sdk=34
